# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: Compile Roshan OS

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        build_type: [Release]
        c_compiler: [gcc, clang, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
          - os: macos-latest
            c_compiler: clang
            cpp_compiler: clang++
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl
          - os: macos-latest
            c_compiler: gcc
          - os: macos-latest
            c_compiler: cl
          - os: ubuntu-latest
            c_compiler: clang

    steps:
    - uses: actions/checkout@v4

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Get Linux Dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt update
        sudo apt install -y libgl1-mesa-dev libxkbcommon-x11-0

    - name: Update aqtinstall for windows
      if: matrix.os == 'windows-latest'
      run: |
        python -m pip install --upgrade aqtinstall

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.9'
        target: 'desktop'

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: |
        cd 'Roshan System - C++ Version'
        mkdir build
        cmake -B build -S . DCMAKE_BUILD_TYPE=Release

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: |
        cd 'Roshan System - C++ Version'
        cmake --build build --config release
    - name: Deploy App for Windows
      if: matrix.os == 'windows-latest'
      run: |
        cd 'Roshan System - C++ Version/build/Release'
        windeployqt RoshanSystem.exe
    - name: Deploy App for Mac
      if: matrix.os == 'macos-latest'
      run: |
        cd 'Roshan System - C++ Version/build'
        macdeployqt RoshanSystem.app
    - name: Deploy App for Linux
      if: matrix.os == 'ubuntu-latest'
      run: |
        cd 'Roshan System - C++ Version'
        wget https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/1-alpha-20250213-1/linuxdeploy-plugin-qt-x86_64.AppImage -o LinuxDeployQt.AppImage
        chmod +x LinuxDeployQt.AppImage

        mkdir -p appdir/usr/bin

        cp build/RoshanSystem appdir/usr/bin
        cp -r build/textures appdir/usr/bin
        cp -r build/styling appdir/usr/bin

        ./LinuxDeployQt.AppImage --appdir appdir --plugin qt --output build/RoshanSystemApp
     
    - name: Upload Built version
      uses: actions/upload-artifact@v4
      with:
        name: RoshanSystem-Package
        path: |
          Roshan System - C++ Version/build/
          Roshan System - C++ Version/build/Release/
          !Roshan System - C++ Version/build/**/*.obj
          !Roshan System - C++ Version/build/**/*.o
          !Roshan System - C++ Version/build/**/*.tlog
          !Roshan System - C++ Version/build/**/CMakeFiles/
          !Roshan System - C++ Version/build/**/CMakeCache.txt
